---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: qcclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: QCClusterTemplate
    listKind: QCClusterTemplateList
    plural: qcclustertemplates
    shortNames:
    - qcct
    singular: qcclustertemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: QCClusterTemplate is the Schema for the qcclustertemplates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: QCClusterTemplateSpec defines the desired state of DOClusterTemplate.
            properties:
              template:
                description: QCClusterTemplateResource contains spec for DOClusterSpec.
                properties:
                  spec:
                    description: QCClusterSpec defines the desired state of QCCluster
                    properties:
                      clusterAutoScale:
                        description: ClusterAutoScale manage the cluster nodes if
                          enabled, it will auto scale the cluster nodes.
                        properties:
                          averageCPUUtilization:
                            description: target average CPU utilization (represented
                              as a percentage of requested CPU) over all the nodes;
                              if not specified the default autoscaling policy will
                              be used.
                            format: int32
                            type: integer
                          enabled:
                            description: Enabled clusterAutoScale, if it's not specified,
                              default false.
                            type: boolean
                          maxReplicas:
                            description: MaxReplicas is the upper limit for the number
                              of replicas to which the clusterAutoScale can scale
                              up. It cannot be less that minReplicas.
                            format: int32
                            type: integer
                          minReplicas:
                            description: MinReplicas is the lower limit for the number
                              of replicas to which the clusterAutoScale can scale
                              down.  It defaults to 1 node.  minReplicas is allowed
                              to be 0.  Scaling is active as long as at least one
                              metric value is available.
                            format: int32
                            type: integer
                        type: object
                      controlPlaneEndpoint:
                        description: ControlPlaneEndpoint represents the endpoint
                          used to communicate with the control plane. If ControlPlaneDNS
                          is unset, the QC load-balancer IP of the Kubernetes API
                          Server is used.
                        properties:
                          host:
                            description: The hostname on which the API server is serving.
                            type: string
                          port:
                            description: The port on which the API server is serving.
                            format: int32
                            type: integer
                        required:
                        - host
                        - port
                        type: object
                      ingress:
                        description: Ingress management Ingress controller in cluster
                          if set to true, it will auto install nginx-ingress in cluster
                        properties:
                          enabled:
                            type: boolean
                        type: object
                      kubesphere:
                        description: KubeSphere is a distributed operating system
                          for cloud-native application management if set to true,
                          it will auto install harbor in cluster
                        properties:
                          enabled:
                            type: boolean
                        type: object
                      network:
                        description: NetworkSpec encapsulates all things related to
                          GCP network.
                        properties:
                          apiServerLoadbalancer:
                            description: Configures an API Server loadbalancers
                            properties:
                              resourceID:
                                description: The QingCloud load balancer ID. If omitted,
                                  a new load balancer will be created.
                                type: string
                            type: object
                          eip:
                            description: EIP configuration.
                            properties:
                              bandwidth:
                                description: Bandwidth defines the EIP bandwidth to
                                  use. default(10M/s).
                                type: integer
                              billingMode:
                                description: BillingMode defines the EIP BillingMode
                                  to use. [bandwidth / traffic] default("traffic").
                                type: string
                              resourceID:
                                description: ResourceID defines the EIP ID to use.
                                  If omitted, a new EIP will be created.
                                type: string
                            type: object
                          securityGroup:
                            description: SecurityGroup defines the SecurityGroup configuration.
                            properties:
                              resourceID:
                                description: The QingCloud load balancer ID. If omitted,
                                  a new load balancer will be created.
                                type: string
                            type: object
                          vpc:
                            description: VPC defines the VPC configuration.
                            properties:
                              reclaimPolicy:
                                description: ResourceID defines the Router ID to use.
                                  If omitted, a new VPC router will be created.
                                type: string
                              resourceID:
                                type: string
                            type: object
                          vxnets:
                            description: VxNets configuration.
                            items:
                              properties:
                                ipNetwork:
                                  type: string
                                resourceID:
                                  description: ResourceID defines the VxNet ID to
                                    use. If omitted, a new VxNet will be created.
                                  type: string
                              type: object
                            type: array
                        type: object
                      repository:
                        description: Repository management image repository in cluster
                          if set to true, it will auto install harbor in cluster
                        properties:
                          enabled:
                            type: boolean
                        type: object
                      storageClass:
                        description: StorageClass management storageClass in cluster
                          if set to true, it will auto install openebs in cluster
                        properties:
                          enabled:
                            type: boolean
                        type: object
                      zone:
                        description: The QingCloud Region the cluster lives in.
                        type: string
                    required:
                    - zone
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
          status:
            description: QCClusterTemplateStatus defines the observed state of QCClusterTemplate
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
